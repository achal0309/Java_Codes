package Basic; import java.util.Scanner; class Basic { public static void main(String[] args) { int index, sindex, number; String temp; Scanner input = new Scanner(System.in); String[] array = new String[5]; //INPUT SHOULD NOT EXCEED 5 System.out.println("ENTER THE ELEMENTS OF ARRAY"); for (index = 0; index < array.length; index++) { array[index] = input.nextLine(); } number = array.length; for (index = 0; index < number - 1; index++) { for (sindex = 0; sindex < (number - index - 1); sindex++) { if (array[sindex].compareToIgnoreCase(array[sindex+1])>0) { temp = array[sindex]; array[sindex] = array[sindex + 1]; array[sindex + 1] = temp; } } } System.out.println("THE SORTED ARRAY IS\n"); for (index = 0; index < number; index++) { System.out.println(array[index]); } } }
